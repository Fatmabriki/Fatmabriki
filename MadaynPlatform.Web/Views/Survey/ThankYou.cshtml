@{
    ViewData["Title"] = "شكراً";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container py-5 text-center">
    <h3 class="mb-3">شكراً لمشاركتك!</h3>
    <p>تم استلام إجاباتك بنجاح.</p>
    <a asp-controller="Survey" asp-action="Index" class="btn btn-primary mt-3">عودة للاستطلاعات</a>
}</div>
